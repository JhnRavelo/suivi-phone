import { StyleSheet } from "react-native";

const buttonStyles = StyleSheet.create({
  buttonContainer: { top: -50, alignItems: "center" },
  buttonView: { borderRadius: 7, padding: 10 },
  buttonText: { fontSize: 26 },
});

export default buttonStyles;
