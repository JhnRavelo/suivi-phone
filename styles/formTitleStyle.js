import React from "react";
import { StyleSheet } from "react-native";

const formTitleStyle = StyleSheet.create({
  titleView: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    width: "auto",
    padding: 15,
    marginBottom: -10,
  },
  titleText: { fontSize: 26, color: "#1C2B39", fontWeight: "900" },
});

export default formTitleStyle;
